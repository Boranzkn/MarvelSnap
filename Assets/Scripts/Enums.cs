public enum CardPositionState
{
    Deck,
    Hand,
    Location
}

public enum CardAbilityType
{
    NoAbility,
    SpecificText,
    OnReveal,
    Ongoing,
    Activate,
    EndOfTurn
}

public enum SortType
{
    Cost,
    Power,
    Name
}